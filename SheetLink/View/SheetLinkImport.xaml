<Window x:Class="PNCA_SheetLink.SheetLink.View.SheetLinkImport"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PNCA_SheetLink.SheetLink.View"
        xmlns:userControls="clr-namespace:PNCA_SheetLink.SheetLink.View"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="SheetLink - Import Schedule"
        Height="250"
        Width="566">
    <!-- Outer border to simulate window frame -->
    <Border Background="#1E1E1E"
            BorderBrush="#3A3A3A"
            BorderThickness="1"
            VerticalAlignment="Top"
            Padding="0,0,0,0">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"></RowDefinition>
                <RowDefinition Height="218"></RowDefinition>
            </Grid.RowDefinitions>
            <local:PNCATitleBar Title="ScheduleSyncer : Export Schedule with Element ID"
                                TitleIcon="pack://application:,,,/PNCA_SheetLink;component/SheetLink/Resources/SheetLinkImport-TitleBar.ico" />

            <local:SheetLinkUserControl Grid.Row="1"
                                        SaveLocation="{Binding FileLocation, Mode=TwoWay}"
                                        FilteredSchedules="{Binding FilteredSchedules}"
                                        SelectedSchedule="{Binding SelectedSchedule, Mode=TwoWay}"
                                        ScheduleSearchText="{Binding ScheduleSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        IsActiveViewSelected="{Binding IsActiveViewSelected, Mode=TwoWay}"
                                        IsSelectScheduleSelected="{Binding IsSelectScheduleSelected, Mode=TwoWay}"
                                        ShouldOpenDropDown="{Binding ShouldOpenDropDown, Mode=TwoWay}"
                                        IOButtonText="Import"
                                        ExportCommand="{Binding ImportCommand}"
                                        CancelCommand="{Binding CancelCommand}"
                                        BrowseSaveLocationCommand="{Binding BrowseFileLocationCommand}" />
        </Grid>

    </Border>
</Window>